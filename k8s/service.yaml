apiVersion: v1
kind: Service
metadata:
  name: email-mcp
  labels:
    app: email-mcp
spec:
  selector:
    app: email-mcp
  ports:
    - name: mcp
      port: 8000
      targetPort: 8000
  type: ClusterIP
